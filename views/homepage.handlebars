<nav class="navbar navbar-dark bg-dark navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid d-flex justify-content-between w-100">
    <a class="navbar-brand m-0" href="#">The Tech Blog</a>
        <div class=" d-flex justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                <a class="nav-link" aria-current="page" href="#">Home</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="#">Dashboard</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="#">Login</a>
                </li>
                <li class="nav-item">
                <a class="nav-link" href="#">Logout</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<div class="container mt-5 postContainer">
    <div class="row row-cols-1">
        {{#each posts as |post|}}
        <div class="col align-self-center py-3 d-flex justify-content-center">
            <div class="card w-75 border-dark">
                <div class="card-header">
                    {{name}}
                </div>
                <div class="card-body">
                    <blockquote class="blockquote mb-0">
                    <p>{{description}}</p>
                    <footer class="blockquote-footer">{{user.name}} <cite title="Source Title">{{format_date date_created}}</cite></footer>
                    </blockquote>
                </div>
            </div>
        </div>
        {{/each}}
    </div>
</div>